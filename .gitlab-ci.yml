stages:
  - build

build-code-job:
  stage: build
  image: node:alpine
  tags:
    - build
  script:
    - cd event
    - npm i
    - npm run build
    - cd ../ticket
    - npm i
    - npm run build
    - cd ../docker-compose
    - docker-compose create --build
